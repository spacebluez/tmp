# dev216：1440万槽，str='S' 与 UInt8 substituted 压测

日期：2026-09-14。

## 一、数据与测试方法

- dev216，ClickHouse 23.12.1.9，8个逻辑CPU。每查询2线程、512MiB；本机HTTP访问，关闭查询结果缓存；未清操作系统缓存。
- 10000仪器×30天×48槽=14,400,000有效槽，是上一轮主规模的10倍。模拟时间为2026年9月，包含测试当天之后的模拟槽。
- 80%仪器用数值val、str为空；20%仪器用业务字符串、val初始为0。字符串含状态、读数和部分长detail，长度28～174字节，平均102.386字节，不是标签集合。
- 10%槽人工修正，val增加25。修正允许命中字符串仪器；这会影响str方案的存储语义和磁盘占用。
- baseline不修正；str_s修正时覆盖str='S'；uint8保留str并将substituted设为1。三表同源，命中槽、修正数值及版本策略一致。
- VersionedCollapsingMergeTree，排序键(__name__,__ts__)，月分区、8192粒度、默认压缩、TTL三年。
- optimize_on_insert=0，旧版本负行+新版本正行；修正表各17,280,000物理行，折叠后14,400,000行；三组未折叠共48,960,000行。
- 每查询条件预热一次、正式12次，方案交错执行，共720次正式查询测量；另做240次单槽修改。
- P50包含HTTP返回和结果解码，不含SSH往返及正确性断言。不是生产容量测试，12次样本不能确定稳定的生产尾延迟。
- 仅操作独立库bench_energy_strs_20260914_large，没有修改业务库、业务代码和服务器全局设置。暂停的测试表合并均已恢复。

## 二、三组磁盘占用

下表已确认全部非活跃旧part自然清理完毕。每表1个活跃part，避免part数量差异影响。
活跃落盘由system.parts.bytes_on_disk统计；实际目录占用由du -s -B1统计，包含文件系统块与目录元数据。单位MiB=1024²字节。

| 方案                  |   有效行数 | 活跃落盘MiB | 实际目录分配MiB | 活跃大小较基准MiB |
| --------------------- | ---------: | ----------: | --------------: | ----------------: |
| 未加人工修正          | 14,400,000 |     328.685 |         328.797 |            +0.000 |
| str='S'               | 14,400,000 |     326.711 |         326.820 |            -1.974 |
| 新增UInt8 substituted | 14,400,000 |     345.480 |         345.594 |           +16.795 |

最终磁盘采样时间：2026-09-14T21:13:46.752725+08:00。

### 新列实际成本分解

UInt8方案相比基准增加约16.795MiB，其中：

- substituted列压缩数据2,480,706字节，约2.366MiB；其逻辑未压缩数据14,400,000字节。
- val列压缩数据增加约9.352MiB。
- version列压缩数据增加约5.075MiB。
- 少量其他文件/元数据差额。

val与version增长来自人工改值、版本分布变化，两种修正方案都承担，不是UInt8独有成本。只增加列且全部为默认0、尚未修正时，整表约328.687MiB，与基准328.685MiB几乎一致。

str方案比UInt8少约18.769MiB，其中约16.403MiB来自覆盖原字符串后str列变小，约2.366MiB来自没有新增标记列。

本次str='S'覆盖了288,000条原字符串，原内容共29,481,840字节。UInt8表保留了原字符串，其字符串总长度及哈希汇总与基准完全一致。str方案保存的信息更少，所以不能把占用较小全部理解为“标记更省空间”。

如果业务仅允许修正原str为空的数值槽，就没有这部分覆盖收益；本报告的空间数字不能直接套用到该不同场景。str方案恢复原字符串所需的外部审计/备份空间未计入。

### 未折叠与临时旧文件

| 方案         | 未折叠物理行数 | 当时活跃落盘MiB |
| ------------ | -------------: | --------------: |
| 未加人工修正 |     14,400,000 |         328.685 |
| str='S'      |     17,280,000 |         380.806 |
| UInt8        |     17,280,000 |         381.876 |

修正表各101个part，包含正负历史和part元数据成本。合并刚结束时，实际目录曾达到约1962MiB、2020MiB，因为旧文件还在；不能把这些临时文件算成永久存储成本。

服务器old_parts_lifetime=480秒，清理周期30秒并有随机附加。没有降低保留时间，也没有手删ClickHouse文件；已等自然清理后再次测量，得到上面的最终目录值。

## 三、判断槽是否锁定

比较三种查询：

```sql
argMax(str, __version__) = 'S'
sum((str = 'S') * __sign__) > 0
sum(substituted * __sign__) > 0
```

均按(__name__,__ts__)分组，HAVING sum(__sign__)>0。前提是每槽最多一个有效版本、取消行复制旧标记；argMax方案还依赖最高版本为当前有效状态。不能先WHERE标记=1再还原槽状态。

已折叠，P50毫秒：

| 场景            | str argMax | UInt8符号求和 |
| --------------- | ---------: | ------------: |
| 单槽            |       5.18 |          4.71 |
| 单表48槽        |       5.21 |          4.69 |
| 单表1440槽      |       7.02 |          6.69 |
| 1000表同一时刻  |      41.12 |         16.20 |
| 10000表同一时刻 |     271.76 |         63.91 |
| 离散1000槽      |     481.28 |        355.33 |
| 离散10000槽     |     896.75 |        685.79 |

10000表同一时刻，UInt8相比str符号求和约快4.04倍。

同时查询用量，10000表同一时刻：str argMax 315.63ms，str求和303.60ms，UInt8 98.63ms。

真正未折叠，P50毫秒：

| 场景            | str argMax | UInt8符号求和 |
| --------------- | ---------: | ------------: |
| 单槽            |       8.92 |          8.96 |
| 10000表同一时刻 |     352.70 |        122.34 |
| 离散10000槽     |    1104.38 |        856.80 |

## 四、修改一个槽

每方案30槽，数值与字符串仪器各半。每槽依次同值锁定、改120、改130、模拟撤销重算，共240次替换。

| 操作            | str='S'读+改写P50/ms | UInt8读+改写P50/ms |
| --------------- | -------------------: | -----------------: |
| same_value_lock |                 9.30 |               9.13 |
| correct120      |                 9.04 |               9.48 |
| correct130      |                 9.82 |               9.93 |
| recompute       |                 9.87 |              10.11 |

差异较小，不能宣称新增列明显加快单槽写入。写法均为读旧值/版本/标记，再同一次INSERT写入旧版本负行与新版本正行。

str方案撤销时不是直接清空str：模拟重算从外部提供原业务字符串；UInt8方案一直保留str。测试没有调用真实计费引擎，没有验证费用、环境和汇总联动，也没有验证真实故障重试。实际业务仍需解决原值来源及重算失败时的状态一致性。

## 五、复核材料与范围

- 全量分批检查每槽只有一个有效版本；查询结果的用量、锁定、数量和唯一性逐行校验。
- 数值修改一致，命中槽一致；UInt8原字符串保留通过汇总校验。
- 查询2线程/512MiB限制未放宽；造数检查至少15GiB磁盘余量、至少1GiB可用内存。
- 数据与压缩结果依赖本次字符串内容和修正比例，不能保证真实设备数据得到相同比例。
- 未计外部审计、原值备份、复制副本、全局query_log占用。
- 独立测试库保留供复核，没有修改生产schema或业务代码。

文件：

- 压测：D:\tmp\energy_strs_large_20260914.py
- 公共HTTP/DDL辅助：D:\tmp\energy_lock_bench_20260914.py
- 磁盘复查：D:\tmp\energy_strs_disk_recheck.py
- 原始结果：D:\tmp\energy_strs_large_results.json
- 汇总：D:\tmp\energy_strs_large_results_summary.json
- 分析：D:\tmp\energy_strs_large_analyze.py

脚本使用固定独立库名与输出目录；再次执行会因已存在而停止，重跑应改用新库名，不能覆盖当前结果。
