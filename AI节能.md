| 方案 | 保存方式 | 优点 | 缺点 | 需求适配度 | 建议 |
|---|---|---|---|---|---|
| 当前方案：单版本 KVS | 一个 `EnergyType` 对应一个 KVS Key；保存能源基本信息和 `usage_points`；更新时直接覆盖整个对象 | 已有实现，可直接复用；开发量最小；读取简单；无需新增数据库表和 DAO | 旧配置被覆盖；没有 `effective_from`；无法还原历史配置；`etag` 未用于更新前置校验；不能完整表达标准点、能力和映射；扫描后写入无法保证多实例并发一致性 | 低。无法可靠满足累计点切换、历史审计、指定范围重算和复杂配置约束 | 不建议作为新配置的权威存储 |
| 关系型版本表 | 在关系型数据库中新增能源类型、标准点、模型能源配置、测点映射及对应修订表；每次修改新增不可变版本 | 支持事务、唯一索引、行锁和条件更新；多实例并发一致性可靠；可按 `effective_from` 查询历史版本；适合历史审计和重算；能可靠实现跨能源点位占用等约束 | 需要新增 DDL、DAO 和数据访问逻辑；需要适配 MariaDB、OpenGauss、KingBase、达梦；开发和数据库集成测试成本最高 | 高。能够完整满足配置版本、生效时间、并发控制、历史审计和重算要求 | 推荐方案 |
| 纯 KVS 多版本记录 | 每个配置版本使用独立复合 Key，例如 `energy_type/{id}/{revision}`、`model_config/{energy}/{model}/{revision}` | 延续现有 KVS 技术；能够保留历史版本；能源类型和模型配置可以独立更新；单条记录较小 | KVS 缺少事务、CAS 和二级索引；跨能源、跨模型约束需要扫描；多实例并发可能写出冲突配置；按时间槽和条件查询不方便 | 中低。可以保存历史，但难以满足强一致性约束 | 不建议 |
| KVS 聚合快照 | 一个能源类型保存标准点、全部模型映射及历史版本；通过整对象 `etag` 更新 | 无需新增关系表；单个能源类型内部可以通过整对象更新保持一致；读取完整能源配置只需一次查询 | 对象随模型数量和历史版本持续膨胀；修改任一模型都要重写整个对象；不同模型修改竞争同一个 `etag`；跨能源点位占用仍需全量扫描；不利于独立查询和分页 | 中。可以满足部分版本需求，但并发和跨能源约束存在风险 | 可作为控制开发量的折中方案 |
