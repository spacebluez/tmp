### 原始需求与技术问题

需求允许修改标准点、模型能力和实际测点映射，并明确规定：

1. 更换唯一累计量实际测点时，从配置生效槽开始使用新测点。
2. 不允许跨配置切换边界计算累计量差值。
3. 新测点的首个有效值只作为后续计算的基线。
4. 配置变更前的表具明细、计费结果等历史事实保持原结果，不自动改写。
5. 用户可以显式发起指定时间范围的历史重算。
6. 同一实际测点不得跨能源类型重复作为标准点映射。
7. 父模型和子模型不得对相同标准点角色保留冲突的有效映射。
8. “能源计量”能力必须与累计点、主要瞬时点映射完整地一起生效。

因此，技术方案需要解决以下问题：

- 如何查询某个半小时时间槽当时生效的配置。
- 如何避免累计点切换前后的读数被错误地计算差值。
- 如何为延迟计算、失败重试和历史重算提供确定的配置依据。
- 如何保留配置修改历史，支持审计和问题定位。
- 如何保证能力和测点映射同时生效。
- 如何在多实例并发修改时，防止跨能源重复占用和父子配置冲突。

> 配置历史和生效时间来自业务需求；采用关系表、KVS 多版本或聚合快照属于技术实现选择。多实例并发控制不是产品直接指定的功能，但它是可靠落实保存约束所必需的工程能力。

| 方案 | 保存方式 | 主要解决的问题 | 优点 | 缺点 | 需求适配度 | 建议 |
|---|---|---|---|---|---|---|
| 当前方案：单版本 KVS | 一个 `EnergyType` 对应一个 KVS Key，保存当前能源信息和 `usage_points`；更新时覆盖原对象 | 解决简单的能源类型 CRUD 和当前配置查询，不解决历史配置和生效时间问题 | 已有实现，可直接复用；开发量最小；读取简单；无需新增数据库表和 DAO | 旧配置被覆盖；没有 `effective_from`；无法查询历史时间槽对应的配置；无法可靠支持累计点切换、延迟重试和历史重算；`etag` 没有作为更新前置条件；扫描后写入无法保证多实例并发一致性 | 低，不能满足配置切换和历史可追溯要求 | 不作为新配置的权威存储 |
| 关系型版本表 | 新增能源类型、标准点、模型能源配置、测点映射及不可变修订表；每次修改新增版本，并记录半小时槽对齐的 `effective_from` | 解决历史配置还原、生效时间、累计点切换、确定性重算、能力与映射原子生效、跨能源占用和多实例并发问题 | 支持事务、唯一索引、行锁和条件更新；能按时间槽高效查询有效版本；历史审计清晰；多实例并发一致性可靠；适合实现 `etag`；可保证能力和映射一起生效 | 需要新增 DDL、DAO 和查询组装逻辑；需要适配 MariaDB、OpenGauss、KingBase、达梦；开发和数据库集成测试成本最高 | 高，能够完整覆盖需求和工程一致性要求 | 推荐 |
| 纯 KVS 多版本记录 | 每个配置版本使用独立复合 Key，例如 `energy_type/{id}/{revision}`、`model_config/{energy}/{model}/{revision}` | 能解决旧版本保留和按版本追溯，但难以解决跨记录事务和并发唯一约束 | 延续现有 KVS 技术；能够保存历史；能源类型和模型配置可独立更新；单条记录较小 | KVS 缺少事务、CAS 和二级索引；查找指定时间槽版本不方便；跨能源占用和父子冲突需要扫描；多个服务实例可能同时写出冲突配置；能力和映射跨 Key 时难以保证原子生效 | 中低，能解决历史保留，但不能可靠落实强一致性约束 | 不建议 |
| KVS 聚合快照 | 一个能源类型保存标准点、全部模型配置和全部历史修订；通过整对象 `etag` 更新 | 解决单个能源类型内部的版本保留和原子更新问题 | 无需新增关系表；单个能源类型内部可以整对象校验和更新；读取完整配置只需一次查询 | 对象随模型数量和历史持续膨胀；修改任一模型都要重写整个对象；不同模型修改竞争同一个 `etag`；跨能源测点占用仍需扫描；独立查询、分页和并发编辑困难 | 中，可以满足部分版本需求，但跨能源约束和长期维护风险较高 | 仅作为压缩一期工作量的折中方案 |
EnergyType 标准定义
        +
ModelEnergyConfig 显式映射
        +
XTwin 当前模型、继承关系和实例事实
        ↓
EffectiveEnergyResolver 动态解析
        ↓
EffectiveMeter 实例级结果
