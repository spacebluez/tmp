先看 61850 自己是怎么做的，再对比。

## 61850 的 Quality 就是"一个整数"，但不是枚举

61850-8-1 把 Quality 映射成一个 13 位的位串，一个字段：

| 位 | 含义 |
|---|---|
| 0–1 | validity：good / invalid / reserved / questionable |
| 2–9 | detailQual 8 个开关：overflow … inaccurate（PDF 的 1–8 就是这 8 位） |
| 10 | **source：0 process / 1 substituted** |
| 11 | test |
| 12 | operatorBlocked |

取代还配了一组 SV 属性 `subEna / subVal / subQ / subID`：取代值带自己的品质 `subQ`，取代后 `q = subQ` 且 bit10 置 1。

所以 61850 用一个整数同时做到了两件事：**测 bit10 一步查出"是否取代"**，**取代和品质互不覆盖**。它没有把 substituted 做成 detailQual 的第 9 个取值，这是刻意的。

## 两个方案对比

方案 A：`quality` 枚举加一个值（比如 9 = 人工修正）。方案 B：`quality` 只放 0–8，取代放 `labels`（现方案）。

| 维度 | A：枚举加值 | B：独立标志 |
|---|---|---|
| 与 61850 一致 | 否。source 不是 detailQual 的取值，61850 里没有"第 9 种品质叫取代" | 是。对应 bit10 |
| 信息保真 | 修正后 `quality=9`，当前值的品质、被替掉的值的品质都没了；无法表达 subQ | 两者都在 |
| 检索 | `quality = 9` 整数等值，快 | `str LIKE '%substituted%'`，慢一些 |
| 与采集侧 0–8 表兼容 | 9 是我们本地加的，xhouse / 采集不认识；对方将来定义 9 就撞 | 不动那张表 |
| 下游判"能不能用" | `quality == 0` 不够，得加"9 也算能用" | `quality == 0` 即可 |
| 将来加 test / blocked | 再加枚举值，组合爆炸（取代+测试、取代+闭锁…） | 加一个标签 |
| 存储 | 复用 `quality` 列 | 用空闲的 `str` 列 |

## "快速检索"这个优势有多大

看我们实际要查"是否取代"的三个地方：

| 查询 | 范围 | 行数 |
|---|---|---|
| 引擎判锁定 | `(name, ts)` 点查，行已经读出来了 | 0 额外 |
| 重算回执 `locked_window_count` | 单序列一个区间 | 一个月 1440 行 |
| 时/天/月 `corrected_count` | v1.1 走 `window_status`，不扫值行 | 不经过这列 |

全部落在 `ORDER BY (__name__, __ts__)` 主键范围内，成本是读这些行，列是 Int 还是 String 在这个量级分不出来。A 换来的速度优势用在一个我们不跑的查询上。

## 如果检索速度是硬要求

照 61850 的办法：**用位，不用枚举值**。两种落地：

1. `usage_points.samples.status Int32`：billing 恒写 0、没人用。按 PDF 内部 flag 布局存 bit1 = substituted，`WHERE bitTest(status, 1)` 和 `= 9` 一样快，`quality` 不动。我之前不建议用它是怕和 `metrics.samples` 的 ResourceStatus 混，注释写清即可。
2. 把 `quality` 列按 61850 扩成 13 位位串，bit10 = substituted。一列搞定，但 `quality` 的含义就从 PDF 的 0–8 变成位串，采集侧不跟着改就对不上。

推荐 1。它保住了 B 的全部优点，又把 A 唯一的优点拿回来了。
