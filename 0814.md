当前 `EnergyType` 只实现了需求 1 的早期简化模型：

```text
能源类型 -> [(物模型, 实际测点)]
```

它能圈定一批设备测点，但不能完整表达需求 1 所要求的能源语义和运行规则。

主要缺口如下。

1. **没有标准测点模型**

当前只有能源类型级 `unit` 和 `UsagePoint{model, name}`，[energy.proto](/D:/xbrother/R5/xtwin-insight/api/insight/v1/energy.proto:22)。

因此无法表达：

- 稳定的标准测点 ID；
- 累计、主要瞬时、过程瞬时角色；
- 每个标准点独立的 `semantic_type` 和单位；
- 实际遥测点与标准测点之间的映射。

单个 `EnergyType.unit` 也无法同时描述电量 `kWh` 和功率 `kW`。

2. **没有物模型能力**

现有结构无法声明物模型支持：

- `METERING`，能源计量；
- `SUPPLY`，供能。

所以无法校验“能力不能为空”“METERING 必须配置累计点和主瞬时点”，也无法在取消计量能力前分析受影响实例和 Measurement。需求对此有明确规定：[问题回应](/D:/xbrother/R5/AI节能/能源标准测点与物模型映射开发问题回应.md:104)。

3. **配置与实际映射混在一起**

`usage_points` 同时承担“标准语义”和“实际测点绑定”，导致标准点没有独立身份。

需求需要的是：

```text
StandardPoint
        ↓
ModelEnergyConfig
        ↓
XTwin telemetry
```

这样不同模型可以把不同实际测点映射到同一个标准语义。现有 `(model, name)` 列表做不到这一点。

4. **不支持动态继承**

billing 先按配置中的模型查询实例，然后再次要求：

```go
meterTwin.Metadata.Model == usagePoint.Model
```

见 [billing.go](/D:/xbrother/R5/xtwin-insight/internal/energy/billing/billing.go:188)。

reminder 也使用同样的精确模型判定，[reminder.go](/D:/xbrother/R5/xtwin-insight/internal/energy/service/reminder.go:236)。

因此父模型配置不会自然作用于继承子模型；而需求明确要求只配置九类父模型，子模型动态继承，不复制配置：[问题回应](/D:/xbrother/R5/AI节能/能源标准测点与物模型映射开发问题回应.md:145)。

5. **保存校验不足**

当前 `upsertEnergyType` 主要校验 `(model, point)` 是否重复，[usage_srv.go](/D:/xbrother/R5/xtwin-insight/internal/energy/service/usage_srv.go:125)。

它没有校验：

- XTwin 模型和遥测点是否存在；
- semantic type 和单位是否匹配；
- 累计点和主要瞬时点是否齐全；
- 父子模型映射冲突；
- 多继承冲突；
- 继承后实际测点的跨能源占用；
- 停用计量能力的影响范围。

6. **没有生效边界**

更新时直接覆盖 KVS 当前值，然后异步同步 reminder，[usage_srv.go](/D:/xbrother/R5/xtwin-insight/internal/energy/service/usage_srv.go:231)。

没有：

```text
active
pending
effective_from
```

所以累计测点一旦修改会立即被消费者读取，无法满足：

- 从生效槽开始使用新测点；
- 不跨配置边界计算差值；
- 新测点首值只作为基线。

这些规则见 [问题回应](/D:/xbrother/R5/AI节能/能源标准测点与物模型映射开发问题回应.md:113)。

7. **`etag` 不能保护映射更新**

当前 `computeEnergyTypeEtag` 只计算 `co2_factor` 和 `tce_factor`，[util.go](/D:/xbrother/R5/xtwin-insight/internal/energy/service/util.go:368)。

修改 `usage_points`、单位或能源类别可能得到相同 etag；`UpdateEnergyType` 也没有执行原子 expected-etag 条件更新。因此并发修改映射时可能发生覆盖。

8. **下游直接依赖旧结构**

`Measurement` 和 `Billing` 仍以 `usage_point` 作为输出字段，[energy.proto](/D:/xbrother/R5/xtwin-insight/api/insight/v1/energy.proto:169)。billing、reminder、漏算检测分别解析 `usage_points`，没有统一 Resolver。

所以即使只给 `EnergyType` 增加字段，下游也不会自动获得继承、角色、单位和生效时间语义。

结论是：**当前实现不是完全没有能源映射能力，而是只能表达“模型到实际测点的扁平绑定”，无法表达需求 1 的标准点、模型能力、动态继承、有效时间和统一解析。**

这并不意味着必须破坏现有 protobuf 契约。合理方向是保留现有字段号和 RPC，在 `EnergyType` 上做追加式扩展，同时新增独立 `ModelEnergyConfig` 和内部 `EffectiveEnergyResolver`；现有字段不删除、不改类型、不改语。
