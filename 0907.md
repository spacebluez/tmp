表具用量与成本技术方案（能效 2.5 需求 1/2/4）——对比现有 billing 链路的新增点

在现有链路（四循环、runOp 流水、usage_points 撤销重写机制、级联物化）不动的前提下，新增以下内容：

表具解析：新增 meters.Resolver，支持 model_bindings 与子模型继承匹配，统一引擎四处圈定与读接口的序列翻译；upsertEnergyType 补齐量纲/单位/父子互斥校验。
存储：不建表不改 DDL。新增 :end_reading 序列持久化每槽尾值；激活 samples.quality（NORMAL/CORRECTED）与 str（单价快照）两列；window_status 扩为六态；KVS 新增修正留痕命名空间。时/天/月复用现有 MV，读时做首尾槽边界修正。
计算规则：窗口改左闭右开；尾值只认 [T−W, T) 内最晚样本（W 默认 10 分钟）；质量四态（NORMAL/MISSING/BAD/BASELINE），坏值不再填 0，坏值槽尾值仍作下一槽基准；删除去毛刺、换表特判、硬编码限值，改读 max_window_increment；新增 RevokeSlots 处理有值变无值的撤销。
人工修正：新增修正（锁定 quality=5 + 留痕）、撤销（解锁 + 单窗重算）、引擎重算尊重锁定值只重刷成本。
成本：已生效 section 锁定不可改；分段/时段/星期统一按窗口起点判定，无匹配返回 FAILED 不再回退首段；成本显式状态 + 单价快照；读侧 pending_recompute / plan_changed 提示。不做绑定时自动补算，历史补算与换绑重刷统一走人工按范围重算。
接口与编排：落地 7 个表具 RPC，proto 新增 2 RPC、1 枚举、注释与校验修订，前置重新生成代码；新增按表具单 op 的 RecomputeMeters；detectLeakLoop 存在性判定改为只看 :consumption，不新增迟到扫描循环。
今日评审结论：换绑方案场景下自动补算会静默改写已出账成本且服务端无法确定正确范围，与原型人工口径冲突，决定本期不实现自动补算，Billing.valid_from 仅落库预留，已同步修订方案 §0/§1.2/§4.5/§6/§7/§8.4/§11/§13。
