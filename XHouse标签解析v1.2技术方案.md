# xhouse 标签解析 v1.2 技术评审

> 文档状态：技术评审稿，2026-09-17。基于《xhouse标签解析v1.1技术评审.md》复制修订，与同目录《xhouse标签解析v1.2.md》同步，不表示功能已经实现或上线。主改造仓库为 xhouse，核算写入联动归 xtwin-insight。
>
> 已确认方向：补齐 xhouse 整体 fingerprint → labels 能力，不再限定核算桶；有映射返回真实 labels，成功回源查无映射时保留数值并返回空 labels。其他业务可以不写映射，不阻塞通用能力启用。采用全量常驻缓存、启动一次加载与未命中批量回源，不在本版评估缓存容量成本。insight 核算写入联动保留自身映射确认与有效版本要求。
>
> 来源：[配套详细方案 v1.2](./xhouse标签解析v1.2.md)、[历史评审稿 v1.1](./xhouse标签解析v1.1技术评审.md)、[历史详细方案 v1.1](./xhouse标签解析v1.1.md)。旧版本及背景说明保留历史语义，本版以缺失返回空 labels、通用接入和已知风险条款为准；具体代码落点见配套详细方案。

[TOC]

## 1. 背景与目标

### 1.1 为什么改

能效业务需要区分自动核算与人工修正的数据，用于来源展示、人工修正保护和统计。即使数值没有变化，例如自动核算的 100 被人工确认为 100，也需要保留人工修正的身份，避免后续自动重算覆盖。

原方案通过新增 `substituted` 字段标记人工取代，但这会为核算业务引入专用存储字段和查询适配，其他业务如果不了解该字段的含义，就无法正确识别数据来源和人工修正状态。因此，本次改用通用标签表达来源，复用现有指纹和标签结构，由 xhouse 统一解析和返回，让其他业务也能通过一致的查询结构获取来源信息，具体业务规则仍由各调用方解释和执行。

目前 xhouse 尚未打通从指纹还原标签并随数值返回的链路，因此本次补齐统一标签解析与输出能力，让业务通过现有查询入口同时取得有效数值及其标签，无需直查 ClickHouse 拼装。没有映射时保留数值并返回空 labels，不添加系统标记，也不据此认定为自动数据。

### 1.2 解决什么问题

- **能识别数据来源**：查询数值时同时返回对应标签，让业务区分自动核算、人工修正等来源；数值不变但来源改变时也能识别。
- **统一获取数值和标签**：业务通过 xhouse 获取有效数据及其标签，无需自行查询 ClickHouse 拼装，同名不同来源的数据也能完整返回。
- **缺少标签不影响数值读取**：没有标签映射时仍返回数值及空 labels；实际查询故障或映射错误仍明确报错。

### 1.3 明确不解决什么

- **不处理人工修正业务**：人工审批、修正保护、操作审计、成本重算及人工槽数统计仍由 insight／billing 负责，xhouse 只提供数值和标签。
- **不改变现有聚合口径**：保持现有数值统计规则，不为混合来源的汇总结果指定单一来源。
- **不自动补齐来源信息**：不推断历史数据或派生结果的来源，也不要求所有业务补写标签映射。
- **不扩展其他业务信息**：成本版本、窗口质量、尾值等不属于本次标签改造范围。

## 2. 现状

### 2.1 当前流程

普通、核算和派生数据分别存放在三个桶中，各自按现有规则写入和查询。目前样本已有指纹，但尚未打通标签还原与返回链路，业务取得数值后仍无法通过标签识别来源。

~~mermaid
flowchart TB
    subgraph normal["普通数据"]
        W1["通用写入<br/>按 name + tags 生成指纹"]
        S1["metrics.samples"]
        Q1["沿用普通明细取点规则"]
        W1 --> S1 --> Q1
    end

    subgraph usage["核算数据"]
        W2["insight 核算写入<br/>当前按 name 生成指纹"]
        S2["usage_points.samples"]
        Q2["折叠查询核算值"]
        W2 --> S2 --> Q2
    end

    subgraph derived["派生数据"]
        W3["expr 派生写入<br/>当前按输出 name 生成指纹"]
        S3["derived.samples"]
        Q3["折叠查询派生值"]
        W3 --> S3 --> Q3
    end

    Q1 --> C["样本转换<br/>尚未根据指纹还原标签"]
    Q2 --> C
    Q3 --> C
    C --> A["API 按 name 组织返回<br/>未填充 labels"]
    A --> B["业务取得数值<br/>无法通过标签识别来源"]
~~

现有指纹字段、标签映射表结构和 API 的 `labels` 字段可以复用，无需另建一套查询接口。派生结果当前不继承上游标签，不能据此识别人工或自动来源。

### 2.2 当前问题

| 问题                       | 影响                                                         |
| -------------------------- | ------------------------------------------------------------ |
| 标签链路未打通             | 写入的标签身份尚未贯通映射维护、查询还原和 API 返回，业务只能取得数值 |
| 核算更新未完整考虑来源变化 | 同值但来源改变时可能被跳过；引入标签后，还需保证有效值与来源对应一致 |
| 返回与错误处理不完整       | 按名称分组不能完整表达同名不同标签的数据；部分查询错误被吞掉，业务可能将失败误认为无数据 |

## 3. 方案设计

### 3.1 改造后流程

普通、核算和派生桶分别取得正确的明细后，统一解析标签，并将数值和标签一起返回给业务。

~~mermaid
flowchart TB
    A["普通桶<br/>沿用现有取点规则"] --> D
    B["核算桶<br/>取得有效值及对应指纹"] --> D
    C["派生桶<br/>保留折叠规则，值与指纹同源"] --> D

    D["统一标签解析<br/>按指纹读取当前桶缓存／映射表"]
    D --> E["按名称和标签分组<br/>返回数值及真实标签或空 labels"]
    E --> F["业务使用标签<br/>来源展示、修正保护与统计"]
~~

映射表与缓存按桶隔离；缺映射时保留数值并返回空 labels，不视为自动来源。xhouse 不解释人工状态，派生结果也不自动继承上游标签。

### 3.2 标签缓存与按需加载

每个 xhouse 实例按桶独立缓存 fingerprint → labels 映射，缓存常驻，不设过期或淘汰机制。

- **启动加载一次**：异步读取全量映射，校验后加入缓存，不阻塞查询，不执行周期扫描。
- **命中直接使用**：未命中时按批次去重回表，将合法映射加入缓存，供后续查询复用。
- **缺失不缓存**：查无映射时保留数值并返回空 labels，后续查询仍可回表获取新映射。
- **失败明确处理**：启动加载失败保留已校验缓存，不自动重跑；查询仍可按需回表，查询故障或非法、冲突映射明确报错。

**映射不可变，人工修复后重建相关缓存；桶重建或进程重启时重新加载。**

### 3.3 数据与 API 变化

#### 3.3.1 持久化与业务写入契约

- 不新增 samples 的人工状态列，继续使用 __fingerprint__。
- 新写入复用 Metric.HashID 的字节编码：FNV-1a-64，输入为 name 后接 LF，再按键排序依次编码 key、LF、value、LF；不是直接哈希 JSON 文本。名称、键、值不含 LF；键非空唯一，值为字符串；保留 aggregators 等内部键，不允许业务占用。
- usage_points.time_series 保留 date Date、fingerprint UInt64、labels String 的字段形状。labels 是 JSON 对象，{} 为明确空标签；date 为映射写入日期，不是样本有效期。业务可不提供映射，查无记录时返回空 labels，不新增伪造映射。
- 同指纹对应的规范化 labels 不可变，相同内容可重复物理行；不同内容不能覆盖。JSON 重复键、非字符串值等不得被宽松解码成成功标签。
- 不设映射 TTL 或 GC，不与 samples 过期联动。核算映射表必须保留可检查的冲突证据，不能用 ReplacingMergeTree、FINAL 或 any(labels) 消除冲突后声称数据合法。

回源与启动加载 SQL 示意如下，数据库名来自校验后的桶配置，占位符按驱动绑定，不拼接请求 SQL：

~~sql
-- 按未命中集合批量回源，省略号表示动态生成占位符，非可直接部署 SQL
SELECT fingerprint, labels
FROM <bucket_database>.time_series
WHERE fingerprint IN (?, ?, ...)
ORDER BY fingerprint;

-- 桶启动时全量加载一次
SELECT fingerprint, labels
FROM <bucket_database>.time_series
ORDER BY fingerprint;
~~

**1. 表归属与维护职责**

本次复用已有 time_series 映射表，补充业务方写入规则，不新增建表或迁移任务。

- 映射表位于样本所属桶的数据库，不是所有业务共用一张全局表；样本与映射写入对应的同一业务库，不跨桶混用。
- usage_points.time_series 的 schema 与核算映射写入由 insight 负责；derived.time_series 的 schema 由对应库的 schema 管理方／部署方维护。
- xhouse 的 readonly 桶只读取，不负责建表或写入映射。
- 业务可以不提供映射记录，但启用解析后必须有可查询的映射表及读取权限。成功查询空表或查无对应记录时返回数值及空 labels；缺表、权限不足或查询失败仍报错。
- derived 接入读取解析不要求 expr 新增标签继承或映射写入，不能因映射表存在就推断派生数据已有真实来源标签。

**2. 业务如何生成映射**

业务端根据序列名称与业务标签集合生成 fingerprint，映射记录与样本记录使用同一个指纹。以下 Metric.HashID(name, labels) 表示复用现有 HashID 编码规则的概念调用，不表示新增同签名函数。

~~text
业务输入：
    name   = "meter_01_energy:consumption"
    labels = {"source": "manual"}
    （source=manual 仅为业务标签示例，实际键值由业务方约定）

计算指纹：
    fingerprint = Metric.HashID(name, labels)

映射记录 → 当前桶数据库.time_series
    date        = 映射写入日期
    fingerprint = 上述指纹
    labels      = 规范化 JSON 字符串，例如 {"source":"manual"}

样本记录 → 当前桶数据库.samples
    __name__        = 上述序列名称
    __fingerprint__ = 上述指纹
    其他字段        = 数值、时间及对应写入契约要求的字段

关联关系：
    samples.__fingerprint__ = time_series.fingerprint
~~

HashID 必须使用方案规定的字节编码，不是直接对 JSON 字符串求哈希；名称、标签键值及保留键校验沿用持久化契约。业务方写入映射表前，须将 labels 按标签键名的字典序升序排列，再序列化为 JSON 对象写入，值均为字符串；排序规则与 HashID 的标签键排序保持一致，不按标签值排序。同一名称与标签集合可供多个时间点复用，不需要每个样本都新增一条映射。date 是映射写入日期，不是样本有效期，读取时不能按样本日期裁剪映射。

明确无业务标签时使用 {}，按该名称与空标签集合计算指纹并维护显式空映射；查无映射与显式空映射对外均返回空 labels，但查无映射不缓存，也不能据此认定数据为自动来源。

**3. 映射写入与复用规则**

对 insight 核算写入，遵循以下规则：

1. **先确认映射，再写样本。** 根据 name 和 labels 计算指纹；映射不存在则写入，已有相同内容则直接复用。确认映射写入成功且 xhouse 可读取后，再写入使用该指纹的样本。
2. **相同映射不重复维护，冲突不覆盖。** 已确认的映射可缓存复用；同一指纹发现不同标签内容时，报错并停止本次写入。
3. **标签变化也算一次更新。** 即使数值不变，只要标签变化，核算结果也要写新版本；撤销旧记录时保留旧指纹。
4. **失败后先检查再重试。** 映射未确认成功时不写样本；样本写入失败时保留映射，重试前检查是否已经写入，避免重复追加。

其他业务可以不写映射；查无映射时，xhouse 返回数值及空 labels，不添加系统标记，也不补写空映射。

#### 3.3.2 标签返回语义

查到合法映射时返回对应 labels；查无映射时保留数值，返回空 labels，不添加系统标签或状态标记。

| 结果                           | 输出                         |
| ------------------------------ | ---------------------------- |
| 合法非空映射                   | 返回真实 labels              |
| 明确空映射 {} 或成功查表无映射 | 返回空 labels                |
| 查询故障、非法／冲突映射       | 整体报错，不用空标签掩盖故障 |

~~json
{"labels":[]}
~~

显式空映射可以缓存，查无映射不缓存、不补写空映射，后续查询仍可回表获取新映射。两者在响应中不作区分，同名且输出标签相同的数据归为一组。空 labels 不代表自动来源，业务不能仅凭空标签判断人工或自动状态；内部过程 tags 不对外输出。

### 3.4 必要时序图

#### 3.4.1 写入时序

~~mermaid
sequenceDiagram
    participant W as insight writer
    participant C as 已确认映射缓存
    participant D as ClickHouse
    W->>W: 确定 name、labels 并计算 fingerprint
    W->>C: 查询并比较内容
    alt 未命中
        W->>W: 进入指纹写入协调范围并重查
        W->>D: 查询既有映射
        alt 不存在
            W->>D: 写入映射
        else 内容冲突或非法
            W-->>W: 报错并停止，不写 samples
        end
        Note over W,D: 仅无冲突分支继续，确认持久化及读路径可见性
        W->>C: 成功后缓存已确认映射
    else 命中但内容不同
        W-->>W: 报错并停止，不写 samples
    end
    Note over W,D: 仅映射确认成功后进入值写入
    W->>D: 写入带 fingerprint 的核算结果及必要撤销行
~~

#### 3.4.2 启动加载与查询时序

~~mermaid
sequenceDiagram
    participant B as 业务调用方
    participant X as xhouse 查询与解析器
    participant C as 桶级全量常驻缓存
    participant D as ClickHouse
    participant S as 桶级启动加载器
    Note over S,D: 桶启动时异步执行一次，不阻塞查询
    S->>D: 流式读取全量映射
    D-->>S: 按 fingerprint 排序的映射行
    S->>C: 完整校验后逐指纹合入
    B->>X: 查询选定普通／核算／派生桶明细
    X->>D: 普通沿用取点，核算先选有效版本，派生保留折叠语义且值与指纹同源
    D-->>X: 样本值及对应 fingerprint
    loop 有界样本批次
        X->>C: 批量读取去重后的指纹
        C-->>X: 合法命中、错误状态、未命中集合
        opt 存在未命中
            X->>D: 批量查询缺失指纹的全部映射行
            D-->>X: 映射结果
            X->>X: 完整解码校验，查无映射返回空 labels
            X->>C: 合入已确认映射或错误状态，不缓存缺失
        end
    end
    alt 全部批次成功
        X-->>B: 按 name 与真实 labels 或空 labels 分组返回
    else 任一批次失败
        X-->>B: 整体错误，不返回部分成功
    end
~~

### 3.5 配置与评审待确认项

| 配置／约定                 | 取值                                    | 状态与约束                                                   |
| -------------------------- | --------------------------------------- | ------------------------------------------------------------ |
| resolve_labels             | 默认 false                              | 拟新增桶级开关；关闭时不创建缓存／启动加载任务，不要求映射表；false 不代表全为自动 |
| collapsing／readonly       | usage_points 均为 true                  | 沿用现有职责，xhouse 不建核算表、不写核算数据                |
| 缓存模式／TTL              | 全量常驻／无 TTL                        | 已确认，不做 LRU 或容量淘汰，不缓存缺失与瞬时错误            |
| labels_load_timeout        | 建议 2m                                 | 拟新增启动加载超时，必须大于 0；需足以完成目标规模加载，待验证确认 |
| 样本批次／启动加载解码批次 | 各建议 1000 条                          | 实现常量建议值，分别指样本数／物理行数，不是限制缓存总容量   |
| 连接资源                   | 启动加载最多 1 个活动读取；回源独立配额 | 回源并发及总连接预算按部署容量确认，样本与映射池分离         |

| 待确认项                 | 责任方              | 影响及通过条件                                               |
| ------------------------ | ------------------- | ------------------------------------------------------------ |
| 业务标签键值             | insight／业务方     | 明确人工、自动标签约定及保留键，不硬编码示例 source=manual   |
| 核算历史来源恢复         | insight／数据责任方 | 恢复真实来源时依据可靠审计，不统一标自动；无映射可返回空 labels，不阻塞通用启用 |
| 核算写入可见性           | insight／部署方     | 新人工标签写入须确认目标读路径可见；不作为不写映射业务读取的前提 |
| 核算 writer 指纹写入协调 | insight             | 覆盖选择写映射的相关 writer，验证同指纹竞争；不要求其他不写映射业务改造 |
| 核算映射 DDL             | insight             | 不因表引擎合并抹去冲突证据，完成历史表核查                   |
| 启动加载与兼容评估       | xhouse／调用方      | 确认加载超时及同名多组、空 labels 处理；不开展全量缓存容量成本评估 |
| billing 业务统计         | billing             | 人工槽数定义、统计及性能由业务方负责；需要时单独联调，不作为 xhouse 改造 MV 或提供人工统计能力的前提 |

## 4. 边界场景

### 4.1 正常场景

| 场景                          | 行为                                                         |
| ----------------------------- | ------------------------------------------------------------ |
| 同名自动与人工点交错          | 返回多标签组，合并后每槽最多一个有效点                       |
| 自动 100 → 人工 100           | labels 身份与版本变化，旧自动点消失，值仍为 100              |
| 人工 100 → 人工 120、标签不变 | fingerprint 不变，version 递增，返回新值                     |
| 标签缓存全命中                | 不查询映射表；样本查询仍正常执行                             |
| 启动加载后新增映射            | 首次查询未命中时回源补缓存，后续命中                         |
| 存在显式 {} 映射              | 作为合法空标签缓存并返回；查无映射也返回空标签，但不缓存     |
| 不提供映射／成功查表无记录    | 数值照常返回，输出空 labels，不报错、不写成功缓存，后续可恢复真实标签 |
| 聚合混合自动／人工            | 总量数值保持既有口径，返回无单一来源标签                     |

### 4.2 异常场景

以下错误映射为拟实施契约，HTTP 沿用 gateway 转换。错误必须经过 Merger 透传，不转换成 EOF，不触发旁路补无标签值。

| 场景                               | 返回／处置                                                   |
| ---------------------------------- | ------------------------------------------------------------ |
| 成功查表但映射缺失                 | 不是异常：保留数值，返回空 labels；不添加系统标记，不缓存缺失 |
| JSON 非法、重复键、非字符串值等    | DataLoss，labels_mapping_invalid；标记对应指纹非法状态       |
| 同指纹对应不同标签                 | DataLoss，labels_mapping_conflict；标记冲突，不能保留旧成功值继续返回 |
| 多存活版本、负净计数或净计数大于 1 | DataLoss，live_version_conflict；不自动选最新                |
| 开启解析但映射表不存在／必需桶缺失 | FailedPrecondition；不能跳过后返回成功空列表                 |
| CK 暂时不可用                      | Unavailable；不缓存瞬时错误，不返回部分成功                  |
| 请求取消／超时                     | Canceled／DeadlineExceeded；关闭本次读取资源                 |
| 启动加载失败                       | 保留已有缓存并告警；不单独导致已命中请求失败，未命中仍正常回源 |
| 启动加载或回源发现某指纹冲突或非法 | 标记该指纹错误并告警，其他已确认指纹仍可使用；后续相关请求失败 |

日志记录桶、阶段、错误类型及必要的定位信息，不输出整批敏感标签；指标不使用 fingerprint 作为高基数维度。需要观测缓存条目／错误状态数、命中率、回源次数与耗时、启动加载耗时与完成状态、物理行／唯一映射数及内存。

### 4.3 重试与幂等

| 操作                   | 重试与幂等规则                                               |
| ---------------------- | ------------------------------------------------------------ |
| 映射写入               | 相同指纹相同内容可复用或重复物理写入；不同内容拒绝，不能通过覆盖“重试成功” |
| 映射写入超时结果不确定 | 不记成功缓存，不发布样本；先查询并确认实际持久化与可见性     |
| samples 写入失败       | 映射可保留；核查槽状态后重试，不重复追加正行                 |
| 映射读取缺失           | 本次成功返回空 labels，不要求立即重试；后续查询仍回源，真实映射出现后可恢复 |
| 数据库暂时不可用       | 按现有退避策略重试；不在解析器中无界循环                     |
| 并发请求同时 miss      | 允许重复批量读取，合入幂等；前台并发有界，不新增跨实例缓存协调 |
| 启动加载与回源同时合入 | 同内容幂等，错误状态优先，后到合法值不能覆盖冲突状态         |
| 启动加载中断           | 保留已完整校验条目，未完成指纹不发布成功；不自动重跑全量加载，后续查询未命中时回表 |
| DataLoss               | 不盲重试，先修复数据与 writer 原因，受控重建缓存后恢复       |

### 4.4 删除、恢复与切换生命周期

| 事件                     | 规则                                                         |
| ------------------------ | ------------------------------------------------------------ |
| 撤销人工修正后可重新核算 | 返回新自动有效值与对应标签，旧人工组不保留该点               |
| 撤销后无有效原始数据     | 不返回该槽，不补 0，不沿用人工值                             |
| 完全撤销后再次生成       | 版本不能从 1 重新开始或复用尚存历史版本，须依据历史水位分配  |
| samples 过期／删除       | 不联动删除映射或缓存条目                                     |
| 正常映射删除或修改       | 不支持；不可变映射只新增，不提供日常覆盖接口                 |
| 请求取消                 | 只取消本次查询及回源，不取消桶级启动加载任务                 |
| 桶关闭                   | 取消启动加载与相关读取、关闭结果集并等待后台退出后释放连接   |
| 进程重启                 | 从空缓存异步加载一次全量映射，不读本地缓存文件；查询可即时回源 |
| 数据库／路由切换         | 销毁旧桶解析器并重建；writer 也重新确认可见性，不能复用旧目标缓存 |
| 多实例切换               | 每实例独立缓存；冷实例可回源，不能依赖另一实例已预热         |
| 映射人工修复             | 映射不可变；人工修复后重建全部相关 insight／xhouse 缓存      |

## 5. 兼容性与影响范围【查询返回Response待定】

### 5.1 API 兼容性

请求字段、proto 结构及现有路径保持不变，但**同一个 name 可能返回多个 TimeSeries**，属于行为兼容变更。客户端需要从“按名称读取单组数据”调整为“遍历全部组，并保留每个点所属组的 labels”。

本版将该变化列为 R1 已知兼容风险，见 §5.5。除下方自动／人工示例外，查无映射与显式空映射按相同输出标签归入同名空标签组；后续补齐非空映射时点会转入对应标签组，客户端应正确替换旧结果，而不是残留旧组或按 name 覆盖。

#### 5.1.1 改造前：按 name 返回一组，无法从响应判断来源

例如，查询序列 meter_01_energy:consumption，得到三个核算点：

| 时间  | 有效值 | 业务上的来源 |
| ----- | -----: | ------------ |
| 09:00 |     10 | 自动核算     |
| 09:30 |     11 | 人工修正     |
| 10:00 |     12 | 自动核算     |

以下为**返回结构示意**，时间使用可读格式，省略其他字段；人工来源用于说明业务场景，不表示当前标签链路已经支持该能力。

~~json
{
  "time_series": [
    {
      "name": "meter_01_energy:consumption",
      "labels": [],
      "columns": ["time", "val"],
      "points": [
        ["09:00", 10],
        ["09:30", 11],
        ["10:00", 12]
      ]
    }
  ]
}
~~

三个点按 name 放在同一个 TimeSeries 中，labels 未填充。客户端可以取得数值，但**不能据此判断 09:30 是人工修正，也不能把空 labels 理解为三个点都是自动核算**。

#### 5.1.2 改造后：同名按 labels 分组，点继承所在组的标签

以下示例已知自动核算使用显式空映射 {}、人工修正使用 source=manual，返回结构示意如下。一般查询中的空 labels 也可能来自映射缺失，不能据此判定自动来源：

~~json
{
  "time_series": [
    {
      "name": "meter_01_energy:consumption",
      "labels": [],
      "columns": ["time", "val"],
      "points": [
        ["09:00", 10],
        ["10:00", 12]
      ]
    },
    {
      "name": "meter_01_energy:consumption",
      "labels": [
        {"key": "source", "value": "manual"}
      ],
      "columns": ["time", "val"],
      "points": [
        ["09:30", 11]
      ]
    }
  ]
}
~~

这里同一个 name 返回两组数据：

- 第一组包含自动核算点，空 labels 来自明确存在的空标签映射。
- 第二组包含人工修正点，该组所有 points 均对应 source=manual。
- 09:30 只出现在人工组中，旧自动点不会在另一组重复返回。
- source=manual 仅为示例，最终标签键值由业务确认；客户端不应依赖分组返回顺序。

#### 5.1.3 客户端如何适配

**原有写法不能继续使用：**

~~text
遍历返回的 TimeSeries：
    seriesByName[series.name] = series
~~

在上述改造后示例中，后一组会覆盖前一组，导致 09:00 和 10:00 的数据丢失；如果组顺序相反，则会丢失人工修正点。

**改造后的处理方式：**

~~text
遍历返回的全部 TimeSeries：
    遍历当前组的 points：
        将当前组的 labels 与该点关联
        追加到该 name 的点集合

按时间排序，形成展示结果
~~

合并后的业务视图为：

| name                        | 时间  | 有效值 | 点对应的 labels     |
| --------------------------- | ----- | -----: | ------------------- |
| meter_01_energy:consumption | 09:00 |     10 | {}                  |
| meter_01_energy:consumption | 09:30 |     11 | {"source":"manual"} |
| meter_01_energy:consumption | 10:00 |     12 | {}                  |

**兼容要求不是禁止客户端按 name 建索引，而是不能让同名的后一组覆盖前一组。** 客户端可以按 name 汇总，但必须保留各点对应的标签。内部 aggregators 等过程 tags 不参与业务分组及对外 labels 输出。

以上示例针对有效明细；聚合结果的空 labels 表示“没有单一来源”，不能套用明细示例判定为自动核算。

billing 如需判断人工状态或统计人工槽数，应读取未经降采样的 QueryMetrics 并解释业务标签，不能从规整点数、重采样数量、标签组数或历史正行数推导。大范围由 billing 按时间分段读取并按 name 与窗口时间去重；xhouse 仅保证明细及 labels 正确，不承担统计口径。

### 5.2 数据兼容性

旧 hashString(name) 与 HashID(name, 空标签) 编码不同，不强制全量重写历史指纹，也不把二者视为相等。通用解析不要求先补齐历史映射：未提供映射时保留数值并返回空 labels；已有显式合法映射按实际旧指纹读取，不强制用新算法重算校验。

需要恢复真实来源时，可依据可靠审计补映射，确认自动的可写 {}，人工按真实状态迁移。同一旧指纹混用自动与人工时，不能统一补一种来源，需要按槽迁移后才能准确表达；来源不明且无映射可继续返回空 labels，不阻塞通用读取。已存在的非法／冲突映射仍须修复，不能以空 labels 掩盖。

其他业务可以保持不写映射，无需统一升级 writer；选择写映射后必须遵守只增、不改、不删及冲突拒绝约定。表不存在不等于无映射记录，启用桶的映射表须可正常查询。

### 5.3 上下游与代码影响

| 范围                  | 改造内容                                                     |
| --------------------- | ------------------------------------------------------------ |
| xtwin-insight         | 核算映射 DDL、写前确认与缓存、跨 writer 协调、旧指纹保留、同值剪枝、版本水位及存量迁移 |
| xhouse ClickHouse 桶  | 普通／核算／派生携带指纹明细共用解析器；核算保留有效版本选择；配置、labels.go／labels_cache.go／启动加载逻辑、转换及生命周期 |
| xhouse 查询框架与 API | 非 EOF 错误透传、tags 传递、按 name + labels 分组及输出      |
| 调用方                | 同名多组适配、标签保留及错误展示；billing 另负责人工来源解释、人工槽数明细统计和业务保护 |
| 部署与运维            | 目标桶映射表可查询、权限、启动加载及错误告警；insight 新标签写入单独验证可见性，本版不含缓存容量成本评估 |

普通桶与派生桶携带指纹的明细标签解析属于本次交付，不再仅作维持原状的回归项；数值、既有选点、聚合和未涉及路径仍回归。不开启解析的桶不得依赖映射表或启动加载任务；公共错误透传修复改变此前“失败却返回空结果”的表现，需要记录兼容影响。

### 5.4 升级与回滚

1. 先部署通用解析及 API 兼容代码，桶级开关保持关闭；明确普通／核算／派生携带指纹明细的接入路径，无指纹路径不扩展。
2. 完成消费者同名多组及空 labels 适配，确认目标桶映射表可查询。其他业务无需补写映射或全量迁移历史数据，不要求全部 writer 同步升级。
3. 按兼容评估启用目标普通／核算／派生桶，验证非空标签、显式空映射及查无映射的空标签响应、冷／热缓存、实际错误透传和启动加载。核算仍验证有效版本正确，派生验证折叠前后有效值与指纹一致。
4. insight 开放新人工标签写入时，单独完成核算 DDL、writer 版本水位、映射确认与可见性、并发协调验证。不得把这一写入前提施加到不写映射的其他业务。
5. 尚无业务依赖新标签语义时可评估关闭开关回退；已有真实标签或空 labels 处理依赖后，不视为无损回退。核算新标签写入后不回滚 name-only writer，应暂停受影响入口、保留正确读取并前向修复。

映射修复与缓存重建按 §4.4 执行，不提供“改库后等缓存自动刷新覆盖”的恢复路径。

### 5.5 已知风险与处理

当前标签写入联动涉及 xhouse 与 billing；derived 读取接入不要求 expr 写入映射。空 labels 可能来自显式空映射，也可能来自业务未提供映射、映射遗漏或暂时不可见，不将 derived 无映射一律认定为写入异常。

| 编号 | 已知风险                         | 产生原因                                                     | 影响                                                         | 本次处理                                                     |
| ---- | -------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| R1   | 同名多 TimeSeries 的行为兼容变化 | 按 name + 输出 labels 分组；非空标签与空标签可以形成不同组；查无映射和显式空映射在同名下归入同一空标签组，后续补映射可能改变分组 | 客户端按 name 覆盖可能丢组，可能把未知来源误判自动，刷新可能残留旧组 | 列为兼容风险评估项；检查消费者遍历合并、逐点标签保留、来源未知处理及结果刷新，验证同名同时间既有选点／冲突处理未被静默改变 |

本次不额外展开缓存容量成本评估，不把所有业务写入契约统一改造作为阻塞条件。

## 6. 验收

### 6.1 测试场景

| 编号 | 测试场景                                                     | 验收标准                                                     |
| ---- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| T01  | 标签乱序、空标签、中文、空字符串值跨 writer 计算指纹         | 与统一 HashID 测试向量一致；非法键／LF／保留键被拒绝         |
| T02  | 自动 100 → 人工 100 → 人工 120                               | 同值标签变化不剪枝；标签不变改值仍增版本；每步仅返回当前有效点及匹配标签 |
| T03  | 撤销、无有效原始数据、完全撤销后重建                         | 返回新自动值或无点，不补 0、不保留旧人工点、不复用历史版本   |
| T04  | 重复正行、多个存活版本及 CK 合并前／部分合并／完全合并       | 合法结果等价，非法状态明确失败，值与指纹不跨版本拼装         |
| T05  | 自动人工交错、多组 API、规整等距候选                         | 不丢组，同槽／网格最多选一点，值与标签来自同一候选，等距选择确定 |
| T06  | 重复映射、不同 JSON 键序、显式 {}                            | 相同语义去重，不放大点数或总量，空映射可正常缓存             |
| T07  | 查无映射、非法 JSON、重复键、非字符串值及冲突                | 查无映射成功返回数值及空 labels；其他实际错误整体失败，不以空 labels 掩盖、不吞成 EOF |
| T08  | 启动空缓存、映射缺失后补写、启动加载后新增映射               | 启动仅全量加载一次；首次查无映射返回空 labels，不缓存缺失；后续未命中批量回源恢复真实标签并补缓存 |
| T09  | 所有指纹命中、大量点共用指纹、批内部分 miss                  | 全命中映射 SQL 为 0；miss 按去重批次查询，不逐点查询         |
| T10  | 同指纹跨启动加载批次、加载超时／中断／迭代错误               | 跨批完整比较；未完成指纹不发布成功；保留已校验缓存，不自动重跑全量加载，未命中查询可回表 |
| T11  | 回源与启动加载并发、映射人工修复后重建缓存、下游修改标签     | 合入幂等，已发现错误优先；人工修复后重建相关缓存并读取修复结果；共享标签不被污染 |
| T12  | 多 writer 同指纹竞争、映射写入超时、可见性延迟               | 相同内容可复用，不同内容拒绝；未确认映射不记成功缓存、不发布样本 |
| T13  | 请求取消、桶关闭、重启、数据库路由切换、多实例冷启动         | 无遗留任务／结果集；目标隔离；取消请求不停止桶级启动加载；切换不串缓存 |
| T14  | 旧指纹、历史来源混用、未提供映射                             | 显式旧映射可读；无映射返回空 labels，不阻塞通用启用；恢复真实来源时按槽迁移，不批量标自动 |
| T15  | xhouse 数值聚合与重采样分支                                  | 原数值 MV 及总量口径不变，明细／规整标签正确、聚合无错误来源继承；不新增人工槽数或相关 MV |
| T16  | 未开启桶、缺必需桶／表、Merger 错误透传                      | 未开启桶无映射依赖；必需资源缺失明确失败，不能旁路补无标签值 |
| T17  | metrics.samples、usage_points.samples、derived.samples 三路明细接入及其他路径回归 | 共用解析及分组；普通沿用取点，核算先选有效版本，派生保留折叠语义且值与指纹同源；无指纹路径不扩展，数值／聚合回归 |
| T18  | xhouse／billing 对应映射遗漏后修复                           | 遗漏时保留数值并返回空 labels；修复后回源返回真实标签并补缓存，后续请求命中，不缓存缺失 |
| T19  | 非空标签、显式空映射与缺映射的同名分组及后续补映射           | 显式空映射与缺映射合入同名空标签组；消费者不覆盖丢组，补映射后刷新正确，不把空标签当自动，既有同名同时间处理无静默改变 |
| T20  | 空标签返回、内部 tags 隔离、启动加载与回源并发               | 查无映射不添加系统标签、不补写空映射、不缓存缺失；内部 tags 不对外输出；实际错误仍透传，读取资源无连接互等 |
| T21  | derived 现有 name-only 指纹、映射前提及三桶隔离              | 使用 expr 现有 hashString(name) 样本验证非空映射、显式空映射及查无映射；不继承上游标签、不自动标为自动来源、不重算指纹；三桶相同指纹的不同映射在冷／热缓存均隔离；关闭无表依赖、开启缺表报错；撤销重写在合并前后值与指纹关联一致 |

**billing 业务联调（独立于 xhouse 验收）**：如业务交付人工槽数功能，由 billing 定义统计口径并基于有效明细及 labels 统计，与手工逐槽结果核对。此项不代表 xhouse 提供人工槽数能力，也不产生 MV 改造要求。

### 6.2 验证方式

- 单元测试覆盖指纹向量、JSON 校验、缓存状态及并发合入、批次边界、迭代错误、分组和内部 tags 隔离。
- 在隔离 ClickHouse 测试库做真实写入、撤销、合并前后、跨节点可见性、超时与重试验证；单元测试不能替代此项，不在生产库进行测试。
- 在同一数据集下比较改造前后数值；在相同有效状态下比较冷缓存、热缓存、启动加载失败后的查询结果。记录映射命中率、回源 SQL 数、启动加载耗时及查询结果；验证异常缺失修复后可恢复真实标签，不开展全量缓存容量成本评估。
- 在 xhouse 仓库按范围执行已有测试，不为本需求新增测试框架：

~~text
go test ./pkg/types/metric/...
go test ./plugins/buckets/clickhouse/...
go test ./pkg/query/...
~~

### 6.3 通过标准与交付条件

1. **正确性**：T01–T21 满足预期。xhouse 验收有效明细、标签解析、返回分组及现有聚合数值正确性，不验收其不提供的人工槽数统计能力。普通／核算／派生携带指纹明细共用解析语义；核算同槽最多一个有效点且真实 CK 合并前后等价；无映射保留数值并返回空 labels，非法／冲突／查询故障仍失败。
2. **缓存行为**：全命中不回源映射表，未命中按批去重回源；完整校验后才缓存真实映射；缺失不缓存，后续补映射能恢复真实标签，启动加载失败保留已校验缓存，不自动重跑全量加载。
3. **已知风险**：R1 同名多组完成消费者兼容评估和 T19 验证。保留全量常驻，不将缓存容量成本评估纳入验收门槛。
4. **兼容与发布**：目标桶可查询，非空与空标签响应及多组适配通过，覆盖显式空映射和查无映射两种情况；不要求其他业务改 writer 或补齐历史数据。核算新标签写入联动须单独通过 DDL、并发协调、版本水位及可见性验证。
5. **评审闭环**：§3.5 与实际接入相关的参数和责任方事项有结论；启动加载超时 2m 为建议值，不宣称已验证。无指纹路径、聚合来源及全量缓存成本不扩展。

交付物包括通用解析代码与测试结果、空标签响应、缺失不缓存与内部 tags 隔离验证、消费者适配及 R1 同名多组风险评估记录、升级／故障恢复说明；insight 核算联动另提供相关代码、DDL、HashID 向量、版本一致性及可见性记录。历史来源按需恢复，不要求全部业务提交映射补齐或容量压测交付物。本稿描述验收要求，不代表测试已执行。
空标签响应示意：
