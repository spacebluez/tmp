# 计量对象 Expr 计算方案说明

## 1. 背景

当前部分计量对象，特别是虚拟表/聚合对象，会通过 `xtwin-expr` 计算 `consumption`：

```text
by_meters
    ↓
生成 Expr 聚合表达式
    ↓
UpdateExpression
    ↓
Expr 按窗口计算
    ↓
写入 ClickHouse derived.samples
    ↓
xhouse 查询
```

## 2. 当前 Expr 支持能力

| 接口 | 作用 |
|---|---|
| `UpdateExpression` | 创建或更新表达式 |
| `UpdateLogicalDeviceEnableExpression` | 启用/禁用表达式 |
| `RecomputeExpression` | 按 UID 和时间范围重算历史窗口 |
| `PreviewExpressionResult` | 临时表达式即时试算，不落库 |
| `GetExpressionResult` | 查询表达式结果 |

当前 `RecomputeExpression` 只能使用指定 UID 当前注册的表达式，不能同时传入历史表达式快照、拓扑版本和目标输出序列。

## 3. 当前问题

当拓扑发生历史变化时：

```text
历史拓扑：对象A = 级联对象a
当前拓扑：对象A = 级联对象a + 级联对象b
```

如果临时修改当前正式表达式后进行历史重算，可能导致：

- 当前实时任务在此期间使用历史表达式；
- 已入队任务执行时读取到错误版本；
- 底层数据修正触发 Expr 自动重算，按当前表达式覆盖历史结果；
- 同一结果序列无法区分不同拓扑版本。

## 4. 方案对比

### 方案一：历史结果独立存储

```text
历史拓扑
    ↓
独立历史 Expr 任务
    ↓
独立历史结果序列
    ↓
insight 按拓扑版本查询
```

优点：不修改当前正式表达式，不影响当前实时计算，版本语义清晰。

缺点：查询侧需要支持按拓扑版本选择结果，并维护历史任务和结果序列。

### 方案二：扩展 Expr 历史快照计算能力

```text
历史表达式快照 + 拓扑版本 + 时间范围 + 输出序列
    ↓
Expr 独立历史任务
    ↓
写入指定结果序列
```

优点：历史表达式、窗口和结果版本明确绑定，由 Expr 统一负责计算和落库，不影响当前正式表达式。

缺点：需要改造 Expr 接口、任务模型和结果存储。

### 方案三：临时修改当前正式表达式

```text
修改当前表达式
    ↓
历史重算
    ↓
恢复当前表达式
```

不建议作为正式方案，只能作为过渡方案。该方案存在当前计算被污染、任务读取错表达式以及历史结果被自动重算覆盖的风险。

### 方案四：Insight 计算历史任务，Expr 只计算当前任务

```text
当前任务：
表具实时数据
    ↓
Expr 当前表达式
    ↓
ClickHouse 当前结果
    ↓
xhouse 查询

历史任务：
历史拓扑版本
    ↓
Insight 按版本读取表具数据
    ↓
Insight 按输入/输出方向计算
    ↓
写入历史结果或历史结果表
    ↓
查询时按拓扑版本读取
```

也可以表示为：

```text
底层表具数据
    ├── 当前窗口 ──> Expr ──> 当前结果
    │
    └── 历史窗口 ──> Insight ──> 历史版本结果
```

优点：

- 不需要修改当前 Expr 表达式；
- 不会影响 Expr 当前任务；
- Insight 已经掌握拓扑版本、输入/输出方向和历史区间；
- 历史计算逻辑与拓扑版本天然一致；
- 可以避免 Expr 自动重算覆盖历史拓扑结果。

缺点：

- Insight 需要自行实现历史窗口计算和结果写入；
- 需要明确历史结果存储格式、幂等、重试和查询路由；
- Insight 和 Expr 可能出现两套计算口径，需要统一公式和数据质量标记；
- 需要处理 Expr 和 Insight 同时消费底层数据修正的情况。

该方案要求严格区分结果序列：

```text
当前结果：objectA:consumption
历史结果：objectA:consumption@topology-v1
```

不能让 Insight 历史计算和 Expr 当前计算写入同一条无版本结果序列。

## 5. 建议决策

推荐优先评估：

```text
方案四：Insight 负责历史拓扑计算，Expr 只负责当前任务
```

原因：

- 改造边界相对清晰；
- 不需要先改造 Expr 的正式表达式和重算接口；
- 历史拓扑解析本来就属于 Insight；
- 可以避免历史拓扑计算污染当前 Expr 任务。

但需要同步确定：

```text
1. 历史结果是否单独存储
2. 历史结果是否需要回写当前结果表
3. Insight 如何写入 ClickHouse
4. xhouse 如何按 topology_version 查询
5. 底层数据修正后，历史任务是否由 Insight 重新计算
6. Insight 与 Expr 的结果格式和质量状态如何统一
```

如果要求历史结果必须直接修复当前结果序列，则方案四仍需要额外的“历史结果发布/回写”机制；否则建议保留独立的版本化历史结果。
