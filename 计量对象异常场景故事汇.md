# 计量对象异常场景故事汇

## 故事一：这块表到底属于谁？

A 机房和 B 机房各有一块电表。实施人员配置时误将 A 机房的电表绑定到了 B 机房。

结果：

```text
A 机房：少了一块表
B 机房：多了一块表
```

最终表现为 A 机房能耗偏低，B 机房能耗偏高。

业务要求：同一能源类型下，一块表不能同时归属多个计量对象；保存时提示表具当前归属对象；如果是历史配置错误，应修改对应历史记录，不能改变过去的统计结果。

## 故事二：一块表从 A 换到了 B

一块总表原来计量 A 配电柜，配电线路改造后改为计量 B 配电柜。现场人员直接把表具从 A 拖到了 B，但没有登记变更时间。

系统无法判断改造前后这块表应该算给谁，可能导致 A 的历史能耗被减少、B 的历史能耗被增加。

正确语义：

```text
改造日前：表具计量 A
改造日后：表具计量 B
```

应登记新的线路版本，从生效日开始使用新关系。

## 故事三：旧表坏了，换了一块新表

A 机房旧电表损坏，现场更换了新电表。如果只删除旧表、添加新表，不记录换表时间，可能出现换表前数据被新表覆盖、换表后数据从错误时间开始统计、新旧表数据重复或累计量跳变。

业务要求：记录旧表、新表和生效时间；换表前后分别使用对应表具；如有累计量衔接要求，记录换表时的初始读数。

## 故事四：下层对象换了上级

原来：

```text
园区
└── A 楼
    └── 机房 1
```

后来机房 1 搬到了 B 楼：

```text
园区
├── A 楼
└── B 楼
    └── 机房 1
```

如果直接修改关系，系统可能把机房 1 的全部历史能耗都算到 B 楼。

正确语义：

```text
搬迁前：机房 1 归属 A 楼
搬迁后：机房 1 归属 B 楼
```

跨越搬迁时间查询时，按时间分段使用对应拓扑。

## 故事五：一个对象拆成了两个对象

原来只有一个“生产车间”，由一块总表计量。后来拆成生产一区和生产二区，但现场没有新增分表。

系统只有总表数据，无法真实知道两个区域各用了多少能耗。

业务上必须区分：有新增分表才能分别计量；没有新增分表只能继续统计总车间；比例分摊必须标记为分摊值，不能伪装成真实表计值。

## 故事六：对象合并了，历史要不要合并？

原来有 A 车间、B 车间，后来合并成“联合车间”。不能简单把 A、B 的历史关系全部改名为联合车间，否则会改变历史数据口径。

需要明确：合并日前继续保留 A、B；合并日后新增联合车间；联合车间历史数据是否允许查询，必须由业务明确汇总规则。

## 故事七：表具方向配反了

对象 A 的正确计算是：

```text
对象 A 能耗 = 输入表具 - 输出表具
```

但实施人员把输出表具也配置成输入，结果会变成输入加输出，导致能耗明显偏大。

业务要求：同一表具不能在同一对象中同时作为输入和输出；方向变更需要校验；历史方向错误时只修正对应历史版本。

## 故事八：同一表具被重复计算

对象 A 已经配置下级对象 B，而 B 下配置了表具 X。后来用户又把表具 X 直接配置给 A：

```text
对象 A
├── 对象 B
│   └── 表具 X
└── 表具 X
```

表具 X 会被计算两次。系统应在保存时阻止下级对象已覆盖的表具再次直接配置到上级对象。

## 故事九：一条表具通过两条路径进入对象

```text
对象 A
├── 对象 B ──> 表具 X
└── 对象 C ──> 表具 X
```

表具 X 通过两条路径进入 A，造成重复计量。系统需要展开完整拓扑，确保同一叶子表具的最终系数绝对值不超过 1。

## 故事十：表具有关系，但没有数据

对象 A 配置了表具 X，关系校验通过，但表具 X 当天没有上报数据。

```text
没有数据 ≠ 能耗为 0
```

应返回 `NO_DATA` 或 `PENDING`，并标识缺失表具。

## 故事十一：部分表具有数据

对象 A 配置了 5 块表，其中 4 块正常、1 块断数。系统需要明确是否返回部分结果、是否标记 `PARTIAL`、成本和能效是否继续计算。

建议返回数值、完整性状态、缺失表具数量和异常表具列表，不能把缺失表具自动补成 0。

## 故事十二：累计量回退或出现负值

表具重启、换表或数据修正后，累计量发生回退。如果直接做首尾差，可能计算出负能耗。

系统需要识别累计量回退、换表、数据补传和异常点，并保留状态标记，不能静默转成正常值。

## 故事十三：线路从单路改成双路

某机柜原来是单路供电，改造后变成双路供电。如果直接修改当前关系，历史页面也会显示成双路供电。

正确做法：改造前为单路供电，改造日登记新的线路记录，改造后为双路供电。供电关系与能耗计量关系可以合并展示，但底层仍是两条独立关系。

## 故事十四：历史拓扑和当前 Expr 同时重算

对象 A 的拓扑发生变化：

```text
历史拓扑：对象 A = 表具 1
当前拓扑：对象 A = 表具 1 + 表具 2
```

此时 Insight 按历史拓扑计算历史数据，底层表具又发生数据修正，Expr 按当前表达式重算同一个窗口。如果两者写入同一个结果序列，历史结果可能被当前结果覆盖。

可选方向：历史结果独立存储并按版本查询；Insight 负责历史计算、Expr 只负责当前计算；或扩展 Expr 支持表达式快照、拓扑版本和独立输出序列。

## 故事十五：删除历史线路记录

用户删除一条历史线路记录。该记录原本负责某个生效区间，删除后上一条记录需要接管这段区间。

系统不能只删除配置，还需要同步处理生效区间、查询口径、历史计算结果、重算任务和版本边界。

## 故事十六：Excel 批量导入配置错误

实施人员通过 Excel 同时配置 100 个对象，其中一块表具被分配给两个对象。

系统不能保存前 99 条、拒绝第 100 条。正确流程是完整校验，发现冲突后整批不保存，并生成包含行号、对象、表具和冲突对象的错误明细。

## 故事十七：历史 Excel 导入

用户打开某个历史线路记录，修改当时的计量关系并重新导入。

系统应只修改目标历史版本，只重算该版本生效区间，不影响其他版本，也不修改当前拓扑。

## 故事十八：设备或组件被删除

拓扑关系中引用的组件后来被删除或更换了实例 ID。此时关系可能仍然存在，但已经找不到对应设备或测点。

系统应显示“关系存在、实例已失效、需要修复”，不能直接当成能耗为 0。

```text
当前配置决定现在怎么算；
历史版本决定过去怎么算；
表具事实决定实际报了什么；
缺数据不能当 0；
当前拓扑不能覆盖历史拓扑。
```
