# AI 节能 v2.5——能源标准测点与物模型映射开发问题回应

> 适用版本：AI 节能 v2.5  
> 关联需求：《基于计费的能耗明细系统需求方案与架构》需求 1“能源标准测点定义与物模型映射”  
> 文档性质：产品口径确认稿  
> 确认日期：2026-08-11

## 一、说明

本文用于统一能源类型、标准测点、物模型能力及实际测点映射相关开发问题的产品口径。

- 本文回答产品范围、业务规则和异常处理口径。
- 明确标注为“技术问题”的事项，由开发在技术方案中确定，产品需求不作实现约束。
- 本次建设不存在存量配置迁移，不定义迁移范围、迁移异常处理或重试规则。
- 本文与主需求方案冲突时，以本文已经确认的专项口径为准，并在后续维护主需求方案时同步收敛。

## 二、问题回应

### Q-01 执行能源类型、标准测点、物模型能力和映射配置的正式角色名称及权限边界是什么？

**回应：**只沿用平台现有菜单级权限。

- 本期不新增专用角色。
- 本期不拆分按钮级或字段级权限。
- 用户具备对应配置菜单的操作权限后，即可按现有菜单权限机制执行能源类型、标准测点、物模型能力和映射配置。

### Q-02 电、水、冷量分别属于“一次能源”还是“工质”？水和冷量是否参与标煤、CO₂ 折算？

**回应：**

- 电属于一次能源。
- 水属于一次能源。
- 冷量属于工质。
- 标煤、CO₂ 折算继续沿用平台既有配置能力与计算规则，本次问题不新增额外折算规则。

### Q-03 标准测点是否具有独立且稳定的技术 ID，ID 如何生成？标准名称、角色和单位被引用后是否允许修改？

**回应：**技术 ID 及其生成方式属于技术实现问题，由开发在技术方案中确定，产品需求不作约束。

标准名称、角色和单位的修改规则见 Q-14：均允许修改；涉及累计量测点变更时，从配置生效槽开始按新配置处理，生效前历史只通过显式重算处理。

### Q-04 一个物模型在某能源类型下是否允许保存为空的支持关系？

**回应：**不允许。

物模型在某能源类型下保存配置时，至少需要选择一项有效支持关系，不保留“显式空配置”状态。

### Q-05 “单位量纲兼容”的单位表和兼容规则是什么？是否允许不同单位归一或换算？

**回应：**本期按简单规则处理。

- 系统默认的电、水、冷量三类能源，其默认单位固定，不允许修改。
- 本期不扩展复杂的跨单位归一、自动换算或单位迁移规则。
- 映射配置应使用与默认能源单位相匹配的物模型测点。

### Q-06 同一“物模型 × 实际测点”能否被不同能源类型复用？

**回应：**原则上禁止同一实际测点跨能源类型重复充当计量标准角色。

保存配置时，如同一实际测点已被其他能源类型用作计量标准角色，应阻止重复配置并提示已有占用关系。

### Q-07 子模型已配置后，再配置父模型并产生冲突时如何处理？

**回应：**移除子模型映射或拦截父模型保存，不应静默自动删除子模型配置。

- 父、子模型不得同时保留相互冲突的有效映射。
- 系统应明确提示冲突的子模型配置。
- 由用户先移除子模型映射后再保存父模型配置，或直接拦截本次保存。
- 系统不得在无用户感知的情况下自动删除或迁移子模型映射。

### Q-08 物模型继承链、对应测点或测点状态发生变化后如何处理？

**回应：**

- 物模型对应测点的一般变化不需要阻止。
- 后续处理和运算以物模型当前对应的测点为准。
- 只有被配置引用的测点被删除时，才视为配置不可继续参与计算。
- 被删除的测点不参与后续能耗明细、指标等处理运算。
- 测点删除不自动修改或清理原始配置，原配置继续保留，以便定位问题和人工处理。
- 后续任务因测点删除而无法计算时，应返回明确错误，不得补零或静默跳过。

### Q-09 “排除名称包含‘联通’的自定义模型”是什么性质的规则？

**回应：**这是标准包默认映射的初始化排除规则，不是永久业务校验。

- 规则仅用于标准包初始化默认映射时排除相关自定义模型。
- 初始化完成后，不持续阻止用户按实际业务需要手工配置。
- 后续升级或运行过程不得通过该规则自动删除、覆盖用户已经完成的配置。

### Q-10 `DCElectricityMeter` 元数据不满足前置条件时，应如何处理标准包发布或初始化？

**回应：**属于技术实现和发布管理问题，由开发在技术方案、标准包校验及发布流程中确定，产品需求不作约束。

### Q-11 旧配置迁移是覆盖全部能源类型，还是只覆盖内置能源类型？

**回应：**本次不存在存量配置迁移问题，不定义迁移范围。

### Q-12 旧配置存在缺失测点、单位不兼容、重复映射或父子冲突时，迁移如何处理？

**回应：**本次不存在存量配置迁移问题，不定义迁移失败、跳过、报表或重试规则。

### Q-13 取消“能源计量”能力时，已有测点映射和实例关系如何处理？

**回应：**能力变更不能自动新增或删除实例关系；取消前必须提示受影响的表具与计量关系。

- 系统应先展示受影响范围。
- 不得因取消物模型的“能源计量”能力而自动删除已有实例关系。
- 相关实例关系由用户显式处理。
- 在影响关系未处理完成前，系统应按页面规则提示并阻止不合法的配置结果生效。

### Q-14 已产生表具明细或计费结果后，是否允许修改标准测点、角色、单位或实际测点？

**回应：**均允许修改。

- 标准测点、角色、单位和实际测点均可按权限修改。
- 更换累计量测点时，从配置生效槽开始使用新测点。
- 不跨配置切换边界计算累计量差值；新测点首个有效值作为后续计算基线。
- 配置生效前的历史保持原处理结果，不自动按新配置改写。
- 如需按新配置处理历史数据，必须由用户显式发起指定时间范围重算。

### Q-15 删除能源类型、标准测点、物模型或实际测点时，已有映射和下游数据如何处理？

**回应：**

#### 1. 能源类型

- 系统默认的电、水、冷量三类能源类型不允许删除。
- 用户新增的能源类型允许删除。
- 删除新增能源类型后，不再生成该能源类型的新表具核算明细，相关计量拓扑关系、计费和指标计算不再生效。
- 删除前已归档的表具核算明细、计费结果等历史事实不级联物理删除，应继续保留用于审计和历史追溯。

#### 2. 标准测点、物模型及实际测点

- 标准测点和实际测点归物模型侧管理，能源模块不限制其删除操作。
- 测点删除后，原能源映射配置不自动改变。
- 被删除测点不再参与后续能耗明细与指标计算；无法计算时应明确报错。
- 物模型或测点的破坏性删除，应先解除相关引用；如果上游模块仍允许删除，则能源模块按“原配置保留、后续不参与计算、明确报错”的规则处理。

#### 3. 总体原则

配置可以停用；已归档事实不级联删除；破坏性删除应先解除引用。

### Q-16 九类默认映射只对标准内置模型生效，还是也为继承子模型生成显式配置？

**回应：**只为九类标准模型生成一份显式配置，继承子模型自动生效，不重复生成显式配置。

- 标准包仅在九类标准模型上初始化默认映射。
- 继承子模型通过模型及测点继承关系自动获得配置效果。
- 不为每个继承子模型重复创建映射记录。
- 子模型未继承所需测点时，不视为有效命中，也不得自动推断或补建测点映射。

## 三、开发执行摘要

1. 权限只沿用菜单级权限。
2. 电、水为一次能源，冷量为工质。
3. 技术 ID 和 `DCElectricityMeter` 发布处理由技术方案确定。
4. 不允许保存空支持关系。
5. 电、水、冷量默认单位固定，本期不扩展复杂单位换算。
6. 同一实际测点原则上禁止跨能源类型重复充当计量标准角色。
7. 父子模型冲突必须由用户处理或拦截，不静默删除。
8. 测点删除后原配置保留，但该测点不再参与计算，计算失败明确报错。
9. “联通”排除仅用于默认映射初始化。
10. 本次无存量迁移。
11. 取消能源计量能力前提示影响范围，不自动增删实例关系。
12. 配置允许修改；累计量测点变更从生效槽开始，历史仅显式重算。
13. 默认三类能源不可删除，新增能源可删除；删除后停止新增计算，历史事实保留。
14. 九类标准模型只生成父级显式配置，继承子模型自动生效。
