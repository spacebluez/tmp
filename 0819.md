# 昨日工作日报（2026年8月18日）

## 今日工作总结

1. 围绕 AI 节能 v2.5，阅读需求基线及需求 1～4 技术设计文档，梳理标准测点映射、表具能耗核算、计量拓扑和电价计算之间的关系与职责边界。
2. 阅读 `xtwin-insight`、`xtwin`、`xtwin-expr` 当前代码，确认现有实现仍以 `usage_points + billing.Engine` 为主，新版标准测点、Resolver、核算事实域尚未正式落地。
3. 在 `AI节能` 目录补充并完善：
   - `代码现状.md`
   - `能源标准测点与仪表能耗核算时序图.md`
4. 补充系统架构图、端到端时序图、核算流程图和数据流转示例，重点说明迟到数据导致的 `t/t+1` 重算、人工修正、计量拓扑正负向关系及费用联动。
5. 明确了几个关键设计结论：首个有效槽只建立基线；缺失或异常数据不能直接按 0 处理；新增模型映射从生效时间开始参与核算；历史数据需要通过显式范围重算处理；`expr` 窗口计算不直接替代表具核算事实域。
6. 完成文档结构、Mermaid 图、引用路径和内容完整性检查，昨日未修改业务代码。

## 遇到的问题

1. 需求设计与当前代码存在较大差距，现有链路仍使用旧的表具配置、固定采样窗口和异常数据清洗逻辑，暂不满足新版核算事实、质量标记、人工修正及历史重算要求。
2. 需求 2、需求 3、需求 4 之间的消费边界较复杂，需要进一步明确事实域、计量拓扑、费用计算和 `expr` 投影之间的接口契约。
3. 对新增模型映射后的历史数据处理方式存在理解偏差，已确认新增映射只从生效槽开始自动核算，历史数据需通过范围重算或人工回填处理。
4. 当前工作主要集中在需求分析和技术方案整理，具体 API、数据表结构及代码实现尚未开始。

## 明日计划

1. 继续评审并冻结需求 3、需求 4 技术方案，重点确认 `InstanceRef`、关系保存、配置生效边界和历史数据重算规则。
2. 编制需求 1、需求 2 的实施计划，明确 API、Proto、存储模型、任务调度和跨服务消费接口。
3. 补充测试方案，覆盖模型继承与测点映射、首槽基线、迟到数据 `t/t+1` 重算、幂等覆盖、人工修正和费用重算。
4. 根据确认后的方案，优先开始 `xtwin-insight` 配置解析及表具核算事实域的实现，并与 `xtwin-expr` 的 `:consumption` 投影保持接口一致。
5. 再次核对远端 R5 环境、分支和工作区状态，为后续开发验证做准备。

注：已核对远程 `192.168.33.216` 的 Codex 会话记录，远端未发现 8 月 18 日新增会话，因此昨日工作内容主要依据本地 Codex 对话和本地项目记录整理。
## 今日工作总结（2026年8月19日）

1. 阅读 AI 节能原型页面及当前 `xtwin-insight` 源码，分析需求 1～6 的现有代码承载能力，形成《需求承载能力分析》文档。
2. 完整阅读需求 1～4 后端技术方案，重点梳理需求 5 的业务目标、依赖数据、对外接口，以及 `expr` Window 的能力边界。
3. 修正并统一技术方案中的关键契约：`preferred_revision`、`cost_revision`、`InstanceRef`、Decimal 金额及成本事实字段。
4. 生成需求 1、2、4 的独立聚合版后端技术设计，补充领域模型、数据流、接口、存储、任务调度、重算、测试和实施顺序。
5. 初步梳理原型页面对后端接口的消费需求。本日主要完成文档分析和方案设计，未修改业务代码、未运行 Go 测试。

## 遇到的问题

1. 当前代码仍以旧的 `usage_points + billing.Engine` 链路为主，需求 1、2、3、5、6 的目标能力尚未落地，需求 4 也需要较大改造。
2. 四份技术方案之间存在修订号类型、表具身份、成本金额类型等契约不一致，已完成初步统一，但还需要在接口文档中再次核对。
3. 需求 5 与 `expr` Window 的职责边界需要进一步明确，尤其是拓扑计算、窗口调度、历史重算和下游指标消费之间的接口关系。
4. 新增接口设计文档尚未完成，外部 API、内部服务接口和模块间 Go 接口还需要统一整理。

## 明日计划

1. 完成需求 1、2、4 的接口设计文档，覆盖 gRPC/HTTP 接口、内部服务接口、Go 模块接口、分页、幂等和错误码。
2. 继续敲定需求 5 技术方案，明确其对需求 1～4 数据和服务的依赖，以及对后续需求的消费契约。
3. 进一步核对原型页面的接口消费，补充对象用量、今日试算、拓扑查询、成本查询和指标生成等场景。
4. 根据聚合技术方案编制实施计划，明确 Proto、存储表、Resolver、核算任务和成本服务的开发顺序。
5. 方案确认后再进入代码实现和针对性测试阶段。
