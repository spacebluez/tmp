## 今日工作总结（2026年8月20日）

1. 阅读需求 1、2、4 聚合技术方案，重点学习价格方案、表具绑定、成本计算、CostFact、CostRollup、Reader、幂等与事件机制。
2. 修正需求 2 的迟到数据重算规则：普通计算只读取上一槽已发布事实；迟到场景按 `t/t+1` 组成重算批次，两个槽全部成功后统一发布，任一失败则全部不发布。
3. 补充批次生命周期接口：`PrepareBatch`、`PublishBatchIfReady`、`FailBatch`，并同步更新相关技术方案和时序图。
4. 完成需求 6“指标选取服务”技术方案，明确其负责指标目录、可选状态、配置缺失和历史引用有效性检查，不负责指标数值计算。
5. 完成需求 3、5、6 聚合后端技术方案，明确 XTwin 拓扑、指标工厂、指标选择服务和 `expr` Window 的职责边界及数据流转关系。
6. 生成《能源计量拓扑、指标工厂与指标选择时序图》，覆盖拓扑配置、指标选择、实时计算、历史重建、今日试算、Watch 缺口和 HA 接管等流程。
7. 编写 Codex 对话历史导出脚本，并生成需求 1、2、4 技术文档阅读历史 Markdown；脚本测试已通过。

## 遇到的问题

1. 当前 `xtwin-insight` 源码仍以旧 `EnergyService`、`billing.Engine` 和 `usage_points` 链路为主，需求 3、5、6 的目标服务和数据模型尚未落地。
2. 需求 3、5、6 涉及拓扑、指标目录、指标值查询和 `expr` Window 多个边界，接口职责和数据快照需要继续统一。
3. “首选用量变化才触发成本计算”的表述容易造成误解，已补充首次核算、迟到重算、人工修正和价格变更等不同触发场景。
4. 对话历史导出过程中曾出现临时 JSON 重复拼接问题，已重新生成输入并完成脚本验证。
5. 今日工作仍以技术方案、接口契约和时序设计为主，尚未进入业务源码开发阶段。

## 明日计划

1. 继续评审并完善需求 3、5、6 聚合技术方案，重点确认拓扑变更、指标重建、实时窗口和历史查询规则。
2. 补充需求 3、5、6 的接口设计，统一 Proto、分页、幂等、错误码、快照和状态枚举。
3. 核对需求 1～6 与原型页面的接口消费，确保指标选择、指标取值、今日试算和历史数据查询能够衔接。
4. 根据聚合方案编制实施计划，明确拓扑服务、指标工厂、指标选择服务和 `expr` 任务改造顺序。
5. 方案冻结后开始拆分 Proto、存储表和服务端代码任务，并补充针对性的契约测试。
## 今日工作总结（2026年8月21日）

1. 阅读 AI 节能原型页面和当前项目源码，梳理现有 `billing` 链路，包括表具任务生成、原始数据查询、用量计算、结果写入和对象级联。
2. 重点分析了房间内两块表具从原始数据进入系统，到 `usage_points.samples` 和 `derived.samples` 的完整数据流转及取数逻辑。
3. 阅读 `expr` Window 计算实现，明确其在派生对象计算、窗口重算、历史补算和结果写入中的职责。
4. 结合 `by_meters`、`by_aggregate` 分析拓扑变更后的历史重算场景，推演了删除计量对象 B 和表具 C 后，计量对象 A 的计算结果变化。
5. 明确当前 KVS、ClickHouse 和 `expr` 的数据职责：
   - KVS 保存当前计量配置和拓扑关系；
   - `usage_points.samples` 保存 billing 生成的表具及计量对象用量明细；
   - `derived.samples` 保存 expr 计算出的派生对象结果。
6. 确认当前实现中拓扑删除、父级引用更新、历史结果清理和全量历史重算之间存在明显边界，完成相关技术方案分析。

## 遇到的问题

1. 当前 billing 主要支持物理表具到计量对象的直接聚合，不能可靠地递归处理多层 `by_aggregate` 拓扑。
2. 删除计量对象或表具后，系统不会自动反向查找并修改上级对象的引用配置，可能导致上级对象继续引用已删除成员。
3. 修改 KVS 中的拓扑配置不会自动修改 ClickHouse 中已有历史结果，必须额外触发历史重算和旧结果替换。
4. 当前 KVS 只保存当前拓扑，没有保存拓扑版本及生效时间，无法严格按照历史时点还原当时的拓扑口径。
5. expr 自动重算通常只覆盖 `rescan_horizon` 范围，更早历史需要手动指定时间范围重算。
6. 今日主要完成代码阅读和方案分析，未修改业务源码，也未完成拓扑变更重算功能实现。

## 明日计划

1. 继续完善拓扑变更后的影响范围计算，明确删除对象、删除表具和父级对象之间的联动规则。
2. 设计拓扑版本、生效时间和历史重算快照，解决历史数据按旧拓扑或新拓扑计算的口径问题。
3. 明确 billing 与 expr 在多级聚合、历史重算、结果撤销和新结果发布方面的接口边界。
4. 补充拓扑变更、依赖对象删除、全量历史重算和旧数据清理的时序图及流程说明。
5. 在方案确认后，拆分拓扑影响分析、引用校验、重算任务和结果一致性相关的实现任务。
